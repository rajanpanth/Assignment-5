# Baseline Build/Test Log

## Rust host build+test (bg be2405q5h, 2026-07-17)
Cmd: `cargo test -p m_ext --features scaled-ui --no-default-features`
- Compiled OK in 2m44s (anchor 0.31.1, anchor-spl 0.31.1, earn @972e896, m_ext 0.2.0). Host rust 1.93. ✓ toolchain works for host analysis.
- Result: 1 passed, **2 FAILED**:
  - `test_calculate_new_index_no_fee` @ conversion.rs:328 — code returns 1777777777775, test asserts 1777777777777.
  - `test_calculate_new_index_with_fee` @ conversion.rs:368 — returns Err InvalidInput (guard line 267): input `last_m_index=1e10 < last_ext_index=1e12` violates domain guard.

### Analysis (verified by hand in python) — NOT a vulnerability
- Case@328 (fee=0, pure u128 integer, platform-independent): true value = le*(nm/lm) = 1.666666666666e12 * (3.2e12/3e12) = 1777777777777.07. Code floors intermediate `m_increase_factor = floor(nm*SCALE/lm) = 1066666666666` then `floor(le*mif/SCALE) = 1777777777775`. So code under-shoots the true floor by 2 due to intermediate truncation. Test's `expected` is stale/wrong (its own comment says "would round up -> truncates").
- Case@368: test inputs use last_m_index=1e10 (=0.01 in 1e12 scale) which is below 1.0 and below last_ext_index; current domain guard rejects. Test predates the guard. Stale.
- **Conclusion:** these are STALE UNIT-TEST EXPECTATIONS at HEAD b69d1f8, in a #[cfg(test)] module = test file (OOS per program rules). The program's rounding is DOWN (ext_index ≤ mathematically-true index), which is protocol-favorable / holder-conservative and matches the acknowledged trunc rounding class. Not eligible.

### Security characterization captured (useful for WS index math)
- `calculate_new_index` loses up to a few index units per call via intermediate `m_increase_factor` floor + final floor. Direction: ext_index ≤ true. Cannot overshoot → cannot cause insolvency via sync.
- `sync` is path-dependent (flooring compounds), but only updates when M's on-chain index actually changes (early-return if latest_m_index==cached). Attacker cannot force extra index steps; number of flooring events bounded by real M index updates, not attacker-controllable. Per-event loss ~ negligible and holder-unfavorable (goes to admin-claimable excess, stays in vault). Weak/known — parked.

## TS jest baseline — BLOCKED
`corepack pnpm jest tests/unit/m_ext.test.ts` fails at import: needs `target/types/{scaled_ui,no_yield,crank,ext_swap}.ts` generated by `anchor build` (not checked in). Requires full BPF build (anchor 0.31.1 + solana 2.1.0). TODO Phase 2.

## Deps
`corepack pnpm install` OK (node_modules populated). pnpm not on bare PATH → use `corepack pnpm`.
